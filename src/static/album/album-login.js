(()=>{'use strict';const form=document.querySelector('#accountLoginForm'),error=document.querySelector('#accountLoginError'),button=form.querySelector('button[type="submit"]'),password=document.querySelector('#accountPassword'),togglePassword=document.querySelector('#toggleAccountPassword');togglePassword.onclick=()=>{const showing=password.type==='text';password.type=showing?'password':'text';togglePassword.textContent=showing?'+':'*';togglePassword.setAttribute('aria-label',showing?'Mostrar contraseña':'Ocultar contraseña');togglePassword.setAttribute('title',showing?'Mostrar contraseña':'Ocultar contraseña');togglePassword.setAttribute('aria-pressed',String(!showing));password.focus()};form.onsubmit=async event=>{event.preventDefault();error.textContent='';button.disabled=true;try{const response=await fetch('/api/album/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:document.querySelector('#accountEmail').value,password:document.querySelector('#accountPassword').value,hp:document.querySelector('#accountWebsite').value})});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||'login_failed');location.assign(payload.album_url)}catch(err){error.textContent=err.message==='try_later'?'Demasiados intentos. Intenta nuevamente más tarde.':'Correo o contraseña incorrectos.';button.disabled=false}}})();
